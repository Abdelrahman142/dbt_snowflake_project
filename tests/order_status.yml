version: 2



models:
  - name: stg_orders
    colunms:
      - name: order_status
        tests:
        - accepted_values: 
            values:['Completed','Panding' , 'Cancelled']
  - name: stg_customers 
    columns:
    - name: customer_id 
      tests:
      - unique